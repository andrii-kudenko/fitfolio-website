/**
 * One source of truth for collection + tier-list item thumbnail size.
 * Update `sizes` if these breakpoints change.
 */
export const itemGridThumbImageSizes =
  '(max-width: 640px) 80px, (max-width: 1024px) 112px, 128px';

export const itemGridThumbWidthClasses =
  'w-20 shrink-0 sm:w-32 md:w-38 lg:w-42';
